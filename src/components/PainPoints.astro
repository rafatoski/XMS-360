---
import { Clock, EyeOff, TrendingDown } from "lucide-react";

const painPoints = [
    {
        icon: Clock,
        title: "Slow & Manual",
        description:
            "Traditional agencies take weeks to launch campaigns. In the AI era, speed is the new currency. If you aren't moving fast, you're already behind.",
    },
    {
        icon: EyeOff,
        title: "Invisible to AI",
        description:
            "Your customers are asking ChatGPT and Perplexity for recommendations. If your brand isn't optimized for Answer Engines (AEO), you don't exist.",
    },
    {
        icon: TrendingDown,
        title: "Guesswork ROI",
        description:
            "Throwing money at ads without clear data attribution is burning cash. We build systems that track every dollar back to a specific outcome.",
    },
];
---

<section id="pain-points" class="py-24 bg-black relative overflow-hidden">
    <!-- Containerized with max-w-7xl -->
    <div class="container mx-auto px-4 max-w-7xl">
        <div class="text-center max-w-3xl mx-auto mb-16">
            <h2
                class="text-4xl md:text-5xl font-display font-bold text-white mb-6"
            >
                The Marketing Landscape Changed. <br />
                <span class="text-accent">Did Your Strategy?</span>
            </h2>
            <p class="text-lg text-muted-foreground leading-relaxed">
                Most businesses are stuck using 2015 marketing tactics in a 2025
                AI world. Manual processes, guesswork campaigns, and zero
                visibility on what drives actual revenue — all while AI-native
                competitors pull ahead.
            </p>
        </div>

        <div class="grid md:grid-cols-3 gap-8">
            {
                painPoints.map((item) => (
                    <div class="group relative p-8 rounded-3xl bg-white/5 border border-white/10 hover:border-accent/50 transition-all duration-300 hover:-translate-y-2">
                        <div class="absolute inset-0 bg-linear-to-b from-accent/5 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-500 rounded-3xl" />

                        <div class="relative z-10">
                            <div class="text-4xl mb-6 bg-white/10 w-16 h-16 rounded-2xl flex items-center justify-center border border-white/5 group-hover:border-accent/50 group-hover:bg-accent/10 transition-colors">
                                <item.icon className="w-8 h-8 text-white" />
                            </div>
                            <h3 class="text-xl font-bold text-white mb-4 font-display">
                                {item.title}
                            </h3>
                            <p class="text-muted-foreground leading-relaxed">
                                {item.description}
                            </p>
                        </div>
                    </div>
                ))
            }
        </div>
    </div>
</section>
