---
import { Search, VolumeX, TrendingDown } from "lucide-react";

const painPoints = [
    {
        icon: Search,
        title: "Invisible to Your Ideal Client.",
        description:
            "Your competitors are dominating search results while your best content remains buried on page 2.",
    },
    {
        icon: VolumeX,
        title: "Messaging That Doesn't Convert.",
        description:
            "You're shouting into the void. Your value proposition is clear to you, but confusing to your market.",
    },
    {
        icon: TrendingDown,
        title: "Burning Budget, Zero ROI.",
        description:
            "Ads are getting more expensive, and traditional strategies are delivering diminishing returns.",
    },
];
---

<section
    class="py-24 md:py-28 bg-black relative overflow-hidden"
    id="pain-points"
>
    {/* Dark Overlay/Background Effects */}
    <div
        class="absolute inset-0 bg-[radial-gradient(ellipse_at_top,_var(--tw-gradient-stops))] from-blue-500/5 via-black to-black opacity-40 pointer-events-none"
    >
    </div>

    <div class="container mx-auto px-4 relative z-10">
        <div class="max-w-3xl mx-auto text-center mb-16">
            <h2
                class="text-2xl md:text-4xl font-bold font-display text-white mb-6 leading-tight"
            >
                The Marketing Landscape Changed. <br />
                <span class="text-blue-500">Did Your Strategy?</span>
            </h2>
            <p class="text-lg text-white/60 font-sans">
                Traditional methods are failing. Using outdated tactics in an
                AI-first world is a choice to be left behind.
            </p>
        </div>

        <div class="grid md:grid-cols-3 gap-8">
            {
                painPoints.map((point) => (
                    <div class="group relative p-8 rounded-2xl bg-white/5 border border-white/10 backdrop-blur-sm hover:bg-white/10 hover:border-blue-500/30 transition-all duration-300 hover:-translate-y-1">
                        <div class="absolute inset-0 bg-gradient-to-b from-white/5 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300 rounded-2xl pointer-events-none" />

                        <div class="w-14 h-14 bg-white/5 rounded-xl flex items-center justify-center mb-6 group-hover:scale-110 transition-transform duration-300 border border-white/5 group-hover:border-blue-500/20">
                            <point.icon className="w-7 h-7 text-white/70 group-hover:text-blue-500 transition-colors" />
                        </div>

                        <h3 class="text-xl font-bold font-display text-white mb-3 group-hover:text-blue-500 transition-colors">
                            {point.title}
                        </h3>
                        <p class="text-white/60 leading-relaxed font-sans">
                            {point.description}
                        </p>
                    </div>
                ))
            }
        </div>

        <div class="mt-20 text-center text-sm text-white/30 font-mono">
            Every tool is a weapon. Together, they're an unstoppable marketing
            machine.
        </div>
    </div>
</section>
